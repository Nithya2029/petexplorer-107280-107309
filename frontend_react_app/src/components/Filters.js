import React from 'react';

// PUBLIC_INTERFACE
function Filters() {
  /**
   * Placeholder for filter/search UI (breed, age, city).
   * Will provide filter controls to PetList in the future.
   */
  return (
    <aside>
      <h2>Filters (Placeholder)</h2>
      {/* Filtering controls will go here */}
    </aside>
  );
}

export default Filters;
