import React from 'react';

// PUBLIC_INTERFACE
function PetList() {
  /**
   * Placeholder component to display a list of pets.
   * Will support filtering, pagination, and grid layout in future implementation.
   */
  return (
    <section>
      <h2>Pet List (Placeholder)</h2>
      {/* Pet cards will be rendered here */}
    </section>
  );
}

export default PetList;
